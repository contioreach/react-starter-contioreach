# Copy to .env and fill in. Every variable here is read in exactly one place —
# server/config.js — which throws a named error at boot when one is missing,
# rather than letting the site 401 on its first visitor.

# --- ContioReach CMS (read API) -------------------------------------------
# Create a workspace at https://app.contioreach.com/signup, then copy the read
# key from Settings → API.
#
# This is the reason the repo has an API server at all: a client-side React app
# cannot hold a secret. Only server/ ever sees these, and server/ is not part
# of the Vite client build.
CMS_API_URL=https://cms-api.contioreach.com
CMS_API_KEY=

# Shared secret your CMS sends with its publish webhook, so the app can tell a
# real invalidation from a stranger hitting POST /api/revalidate/all.
# Settings → Webhooks.
REVALIDATION_SECRET=

# --- This site -------------------------------------------------------------
# The PUBLIC_ four are served to the browser at runtime from GET /api/config,
# not baked into the bundle — so one build artifact runs anywhere.

# Canonical origin: canonical tags, Open Graph URLs, JSON-LD and the sitemap.
# Set it to your real domain before deploying.
PUBLIC_SITE_URL=http://localhost:3000

# Search engines are turned away (noindex + robots.txt Disallow) unless this is
# exactly "true". Leave it false for demo deployments.
PUBLIC_ALLOW_INDEXING=false

# --- Links -----------------------------------------------------------------
PUBLIC_SIGNUP_URL=https://app.contioreach.com/signup
PUBLIC_LOGIN_URL=https://app.contioreach.com/login

# --- Server ----------------------------------------------------------------
# The API server's port. In development the Vite dev server proxies /api here.
PORT=3000
